# =============================================================================
# Wanda — Environment Variables (.env.example)
# =============================================================================
# Copy this file to .env and fill in the values.
# .env is for BOOTSTRAP CONFIG ONLY. API secrets go in the encrypted store.

# --- Telegram ---
BOT_TOKEN=your-telegram-bot-token-here

# --- Admin Identity (Telegram) ---
ADMIN_TELEGRAM_ID=your-telegram-user-id
ADMIN_TELEGRAM_CHAT_ID=your-telegram-chat-id

# --- Secrets Encryption ---
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
WANDA_SECRETS_MASTER_KEY=generate-a-64-char-hex-key

# --- LLM Provider ---
# Stored via secret store, but needed at bootstrap for initial setup
ANTHROPIC_API_KEY=sk-ant-your-key-here

# --- Optional ---
# LOG_LEVEL=info
# NODE_ENV=production
# WEBCHAT_PORT=3000
# WEBCHAT_PORT_RETRIES=10
# WANDA_MAX_OUTPUT_TOKENS=2048
# WANDA_MAX_OUTPUT_TOKENS_OPENAI=1600
# WANDA_MAX_OUTPUT_TOKENS_ANTHROPIC=2048
# WANDA_MAX_OUTPUT_TOKENS_GEMINI=1400
